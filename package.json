{
  "name": "aryan-mudgal-portfolio",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/prepare-sites-build.mjs",
    "preview": "astro preview",
    "test": "node --test tests/portfolio-source.test.mjs",
    "test:build": "node --test tests/portfolio-build.test.mjs",
    "check": "npm run test && npm run build && npm run test:build"
  },
  "devDependencies": {
    "astro": "^7.1.3"
  }
}
